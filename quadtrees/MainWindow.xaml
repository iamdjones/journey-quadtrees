<Window x:Class="quadtrees.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:quadtrees"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Content="Generate Image" Command="{Binding GenerateImageCommand}"/>
            <TextBox Text="{Binding BlueFormula}" />
            <TextBox Text="{Binding GreenFormula}" />
            <TextBox Text="{Binding RedFormula}" />
            <TextBox Text="{Binding AlphaFormula}" />
            <TextBlock Text="{Binding ImageStatus}"/>
        </StackPanel>
        <Image Name="image" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Source="{Binding Image}"/>
    </StackPanel>
</Window>
